import BaseWindow from '../BaseWindow';

export default class MainWindow extends BaseWindow {
  // addSpan(spanText: string, spanId: string) {
  //   const span = document.createElement('span');
  //   span.innerHTML = spanText;
  //   span.classList.add('span-about');
  //   span.setAttribute('id', spanId);
  //   this.element.appendChild(span);
  // }

  // addLink(linkText: string) {
  //   const a = document.createElement('a');
  //   a.innerHTML = linkText;
  //   a.classList.add('span-about');
  //   a.setAttribute('href', 'https://github.com/comtvset');
  //   this.element.appendChild(a);
  // }
}
